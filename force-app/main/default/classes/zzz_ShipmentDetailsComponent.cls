@isTest
private class zzz_ShipmentDetailsComponent {
/* TODO: Write proper testing when logic is landed. This test has sole focus on line coverage */
    static testMethod void codeCoverageTest() {
        ShipmentDetailsComponent sdc = new ShipmentDetailsComponent();
        sdc.getWrapper();
        sdc.setWrapper(null);
        sdc.getShipment();
    }
}